(async()=>{const box=document.getElementById('history');try{const a=await BBDB.cloudHistory();box.innerHTML=a.length?a.map(w=>'<div class="historySession"><b>'+w.name+'</b><p class="muted">'+w.actual_date+' · '+w.status+'</p><div class="historySets">'+(w.workout_exercises||[]).map(e=>'<div><b>'+e.performed_name+'</b>: '+(e.sets||[]).filter(s=>s.is_completed).sort((a,b)=>a.set_no-b.set_no).map(s=>(s.load_value==null?'BW':s.load_value+' '+(s.load_unit||'kg'))+' × '+s.reps+(s.rir==null?'':' @'+s.rir)).join(' · ')+'</div>').join('')+'</div></div>').join(''):'<p class="muted">No completed sessions yet.</p>'}catch(e){box.innerHTML='<p class="muted">'+e.message+'</p>'}})();