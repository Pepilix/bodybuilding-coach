const esc=s=>String(s??'').replace(/&/g,'&amp;').replace(/</g,'&lt;');async function load(){const u=await BBDB.user();if(!u){location.href='index.html';return}if(await BBDB.role()!=='coach'){location.href='index.html';return}const a=await BBDB.coachClients(),active=a.filter(x=>x.status==='active').length,pending=a.filter(x=>x.status==='invited').length,attention=a.filter(x=>x.client_user_id&&(!x.onboarding_complete||!x.last_workout_at)).length;document.getElementById('coachStats').innerHTML=[['ACTIVE',active],['INVITED',pending],['NEEDS SETUP',attention]].map(x=>'<div class="metric"><span>'+x[0]+'</span><b>'+x[1]+'</b></div>').join('');document.getElementById('clientCount').textContent=a.length+' total';document.getElementById('clients').innerHTML=a.length?a.map(x=>'<a class="clientrow" href="'+(x.client_user_id?'client.html?id='+x.client_user_id:'#')+'"><div><b>'+esc(x.display_name||x.email||'Invited client')+'</b><small>'+esc(x.primary_goal|| (x.status==='invited'?'Awaiting signup':'Onboarding incomplete'))+'</small></div><div class="right"><span class="statuspill">'+esc(x.status)+'</span><small>'+(x.latest_weight?x.latest_weight+' kg':'')+'</small></div></a>').join(''):'<p class="muted">No clients yet.</p>'}
load();